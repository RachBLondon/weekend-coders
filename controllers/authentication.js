exports.signup = function(req, res, next){
    //Check if user with email already exisists

    // if users with email does exist return an error

    // if user does not exist create and save a user record

    // respond to say user login was created
}